/*npm y nvm.*/


/**1 sistema basado en  eventos
 *                      asíncrona,
 *                      No bloqueante
 * JavaScript V8 de Chrome. 
 * paquetes de Node.js, ver npm
 * Ryan Dahl en 2009
 * dispara un evento dentro de un único hilo en ejecución
    del tipo asíncrono del Node.js. Esto le permite soportar decenas
    de miles de conexiones concurrentes.
 *//**
                 http://nodejs.org/api/
 * This function multiplies two numbers together.
 * @param {number} x - The first number to multiply.
 * @param {number} y - The second number to multiply.
 * @returns {number} The result of multiplying x and y.
 */
function multiply(x, y) {
    return x * y;
  }
  
  console.log(multiply(2, 3)); // output: 6
 